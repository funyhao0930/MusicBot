# MusicBot 播放控制列設計驗收

- 驗收日期：2026-08-31
- 參考來源：使用者提供的「照片 1.jpg」至「照片 4.jpg」；重點為進度條與時間列、五鍵順序、大型圓形主播放鍵，以及 shuffle／repeat 狀態。
- 實作範圍：本機 Web UI 的正在播放卡片；保留原有深色紫系、歌曲資訊與右側播放佇列。
- 比對方式：將「照片 1.jpg」與 1280 x 720 的控制列實測截圖放在同一個視覺比對輸入中，並另以 390 x 844 驗證窄螢幕。

## 已驗證畫面與互動

| 狀態 | 視窗 | 驗證結果 |
| --- | --- | --- |
| 預設播放 | 1280 x 720 | 進度條置頂、時間左右對齊；隨機、上一首、暫停、下一首、循環依序置中，主鍵為白色圓形。 |
| 隨機＋全部循環 | 1280 x 720 | 隨機與全部循環均顯示狀態點；循環按鈕顯示活躍外框，佇列順序隨 shuffle 更新。 |
| 暫停＋單曲循環 | 1280 x 720 | 主鍵改用本機 `icon-play.svg`，循環改用本機 `icon-repeat-one.svg`，ARIA 標籤同步更新。 |
| 窄螢幕 | 390 x 844 | 五鍵順序未變，中央主鍵與間距縮小；音量、停止與播放佇列仍可讀且未產生控制列溢出。 |
| 播放清單歌名載入 | 1469 x 1208 | 54 首歌名非同步載入時只更新對應列，不重建整個清單，因此不會持續觸發整頁／全清單閃爍。 |

## 比對結論

- P0：無。五個控制項、進度資訊與主播放鍵均已落地，且能互動。
- P1：無。停用上一首、狀態切換、鍵盤可聚焦按鈕、繁中 ARIA 標籤與 tooltip 均可用。
- P2：無。參考圖的酒紅背景未照抄，因為本次規格要求保留 MusicBot 既有深色紫系；這是刻意保留的產品主題，而非比對差異。

Final result: passed
